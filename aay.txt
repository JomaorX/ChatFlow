.messages {
    flex-grow: 1;
    overflow-y: auto;
    display: flex;
    flex-direction: column-reverse;
    padding: 10px;
    gap: 10px;
}

.messages p {
    margin: 0;
    padding: 12px 12px 3px 12px;
    border-radius: 8px;
    max-width: 80%;
    word-wrap: break-word;
    white-space: pre-wrap;
    overflow-wrap: break-word;
    font-size: 1rem;
    position: relative; /* Esencial para posicionar el pseudo-elemento */
    box-sizing: border-box;
    cursor: pointer;
    user-select: text;
    display: flex;
    flex-direction: column;
}

.messages .message-sent {
    background-color: #dcf8c6;
    align-self: flex-end;
    color: #000;
    /* Redondea solo las esquinas que no tienen la punta */
    border-radius: 8px 0 8px 8px; /* ¡IMPORTANTE! Cambio en border-radius */
}

.messages .message-sent::after {
    content: "";
    position: absolute;
    top: 0; /* Cambio para posicionar en la parte superior */
    right: -7px; /* Posiciona la punta fuera del borde derecho */
    width: 0;
    height: 0;
    /* Crea un triángulo con bordes transparentes */
    border: 10px solid transparent;
    border-top-color: #dcf8c6; /* Usa el color del fondo del mensaje */
    border-left-color: #dcf8c6;
    border-right-color: transparent;
    border-top-left-radius: 10px;
}

.messages .message-received {
    background-color: #f0f0f0;
    align-self: flex-start;
    color: #000;
    /* Redondea solo las esquinas que no tienen la punta */
    border-radius: 0 8px 8px 8px; /* ¡IMPORTANTE! Cambio en border-radius */
}

.messages .message-received::after {
    content: "";
    position: absolute;
    top: 0; /* Cambio para posicionar en la parte superior */
    left: -7px; /* Posiciona la punta fuera del borde izquierdo */
    width: 0;
    height: 0;
    /* Crea un triángulo con bordes transparentes */
    border: 10px solid transparent;
    border-top-color: #f0f0f0; /* Usa el color del fondo del mensaje */
    border-right-color: #f0f0f0;
    border-left-color: transparent;
    border-top-right-radius: 10px;
}

.messages p .time {
    cursor: default; 
    user-select: none;
    font-size: 0.8rem;
    color: #666;
    align-self: flex-end;
    margin-top: 5px;
    margin-left: 20px;
}